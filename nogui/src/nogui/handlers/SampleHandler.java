package nogui.handlers;

import org.eclipse.core.commands.AbstractHandler;
import org.eclipse.core.commands.ExecutionEvent;
import org.eclipse.core.commands.ExecutionException;

public class SampleHandler extends AbstractHandler {

	@Override
	public Object execute(ExecutionEvent event) throws ExecutionException {

		
//		String[] codeParts = editorContent.split("\\.");
//		
//		for (int i = 0; i < codeParts.length; i++) {
//			while (java.lang.Character.isWhitespace(codeParts[i].charAt(0)))  {
//				codeParts[i] = codeParts[i].substring(1);				
//			}
//			
//			
//		}
		
		
	
		
//		IWorkbenchWindow window = HandlerUtil.getActiveWorkbenchWindowChecked(event);
//		MessageDialog.openInformation(window.getShell(), "Nogui",	 "Hello, Eclipse world");
		return null;
	}
}
